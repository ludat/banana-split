helm:
  repoURL: "ghcr.io/ludat/charts"
  chart: banana-split
  path: ""
  targetRevision: "0.2.0"

ingress:
  enabled: true
  hosts:
    - host: stg.split.ludat.io
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: bananasplit-stg-http-tls
      hosts:
        - stg.split.ludat.io
