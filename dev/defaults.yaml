helm:
  repoURL: "https://github.com/ludat/banana-split.git"
  chart: ""
  path: "charts/banana-split"

ingress:
  enabled: true
  hosts:
    - host: dev.split.ludat.io
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: bananasplit-dev-http-tls
      hosts:
        - dev.split.ludat.io
