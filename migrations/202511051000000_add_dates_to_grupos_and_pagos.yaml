operations:
  - add_column:
      table: grupos
      column:
        name: fecha
        type: timestamp
        nullable: false
        default: NOW()
  - add_column:
      table: pagos
      column:
        name: fecha
        type: timestamp
        nullable: false
        default: NOW()
